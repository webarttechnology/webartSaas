{

    "scripts": {
        "start": "php -S localhost:8000 -t public -d max_execution_time=6000",
        "watch": "php-watcher --watch=app,public --port=8000"
    },
    "require": {
        "twig/twig": "^3.0",
        "php": ">=7.4",
        "vlucas/phpdotenv": "^5.6",
        "phpmailer/phpmailer": "^6.9"
    },
    "autoload": {
        "psr-4": {
            "Core\\": "core/",
            "App\\": "app/"
        }
    }
}
